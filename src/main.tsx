
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// Creamos la raíz de la aplicación y renderizamos
createRoot(document.getElementById("root")!).render(<App />);
